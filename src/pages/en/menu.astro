---
import BaseLayout from "../../layouts/BaseLayout.astro";
import MenuExplorer from "../../components/sections/MenuExplorer.astro";
import { site } from "../../data/site";

const lang = "en";
---

<BaseLayout lang={lang} path="/menu" title={`Menu | ${site.name} Rome`} description="Full Pomodorum menu with pasta fresca, pinsa romana, salads, sides and dessert.">
  <MenuExplorer lang={lang} />
</BaseLayout>
