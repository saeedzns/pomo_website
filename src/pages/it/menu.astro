---
import BaseLayout from "../../layouts/BaseLayout.astro";
import MenuExplorer from "../../components/sections/MenuExplorer.astro";
import { site } from "../../data/site";

const lang = "it";
---

<BaseLayout lang={lang} path="/menu" title={`Menu | ${site.name} Roma`} description="Menu Pomodorum con pasta fresca, pinsa romana, insalate, fritti e dessert.">
  <MenuExplorer lang={lang} />
</BaseLayout>
