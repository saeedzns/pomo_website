---
import { dictionary } from "../../data/i18n";
import { site, type Language } from "../../data/site";
import LanguageSwitcher from "../ui/LanguageSwitcher.astro";

interface Props {
  lang: Language;
  path?: string;
}

const { lang, path = "" } = Astro.props;
const nav = dictionary[lang].nav;
const links = [
  { href: `/${lang}`, label: nav.home },
  { href: `/${lang}/menu`, label: nav.menu },
  { href: `/${lang}/gallery`, label: nav.gallery },
  { href: `/${lang}/about`, label: nav.about },
  { href: `/${lang}/contact`, label: nav.contact },
];
---

<header class="sticky top-0 z-50 border-b border-ink/10 bg-cream/95 backdrop-blur">
  <nav class="container-page flex min-h-16 items-center justify-between gap-4">
    <a href={`/${lang}`} class="flex items-center gap-3" aria-label={`${site.name} home`}>
      <img src="/images/pomodorum-logo.jpg" alt={site.name} class="h-10 w-auto rounded-md" />
    </a>
    <div class="hidden items-center gap-1 md:flex">
      {links.map((link) => (
        <a class="rounded-full px-3 py-2 text-xs font-bold uppercase tracking-[0.16em] text-ink/70 hover:bg-white hover:text-tomato" href={link.href}>
          {link.label}
        </a>
      ))}
    </div>
    <div class="flex items-center gap-2">
      <a class="hidden rounded-full bg-ink px-4 py-2 text-xs font-bold uppercase tracking-[0.16em] text-white sm:inline-flex" href={site.phoneHref}>
        {dictionary[lang].cta.call}
      </a>
      <LanguageSwitcher lang={lang} path={path} />
    </div>
  </nav>
  <div class="container-page flex gap-1 overflow-x-auto pb-3 md:hidden">
    {links.map((link) => (
      <a class="shrink-0 rounded-full px-3 py-2 text-xs font-bold uppercase tracking-[0.14em] text-ink/70 hover:bg-white" href={link.href}>
        {link.label}
      </a>
    ))}
  </div>
</header>
